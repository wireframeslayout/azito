/**
 * 承認済みトランスクリプト表示スタイル・モックアップ由来の意味色で、既存デザイントークンに
 * 対応がないもの。ここに定数化し、直接のハードコードを避ける。
 *
 * 出典: 本機能（4種の表示スタイル切替）の承認済みモックアップ仕様。
 * 緑（assistant グリフ）・黄（tool グリフ）はモックアップの Aurora 値がそれぞれ
 * `--success` (#3fb950) / `--warning` (#d29922) と一致するため、既存トークンをそのまま使う
 * （ここでの定数化は不要）。magenta のみトークンに対応する色が無いため定数化する。
 */

/** thinking チップ展開時の左髪線（C2）。モックアップ指定値 rgba(255,154,193,0.35) 相当。 */
export const THINKING_HAIRLINE_COLOR = 'rgba(255, 154, 193, 0.35)';
