import { SectionHeader } from '@azito/frontend';

export const Basic = () => (
  <div style={{ maxWidth: 320 }}>
    <SectionHeader>Active windows</SectionHeader>
    <div style={{ fontSize: 13 }}>server01 · 3 sessions</div>
  </div>
);
